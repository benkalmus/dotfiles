# Raise max user processes for Go workloads and other concurrent programs
# Inspect with: ulimit -u
# Apply per-session: prlimit --pid=$$ --nproc=65536
# Apply permanently: replace '*' with your username or keep '*' for all users

*  soft  nproc  65536
*  hard  nproc  65536
